# This is an example file. Copy this file to a new file named .env
# and fill in your actual credentials.
# The .env file should NOT be committed to version control.

# --- OpenAI API Credentials ---
# You can get your API key from the OpenAI platform dashboard:
# https://platform.openai.com/account/api-keys
OPENAI_API_KEY="your_openai_api_key_here"


# --- Reddit API Credentials ---
# You need to create a 'script' type application on Reddit to get these.
# Go to: https://www.reddit.com/prefs/apps
# 1. Click "are you a developer? create an app..."
# 2. Name your app (e.g., "ProjectProspector")
# 3. Select "script" as the app type.
# 4. Add a description and about url (can be anything, e.g., a GitHub repo link).
# 5. The redirect URI can be http://localhost:8080

# The client ID is the string of characters located under your app's name.
REDDIT_CLIENT_ID="your_reddit_client_id_here"

# The client secret is the string labeled "secret".
REDDIT_CLIENT_SECRET="your_reddit_client_secret_here"

# The user agent should be a unique and descriptive string.
# A common format is: <platform>:<app_id>:<version> (by /u/<your_reddit_username>)
# Example: "Python:ProjectProspector:v1.0 (by /u/your_username)"
REDDIT_USER_AGENT="Python:ProjectProspector:v1.0 (by /u/your_username)"